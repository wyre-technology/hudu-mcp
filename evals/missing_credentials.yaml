name: hudu_test_connection returns is_error when credentials missing
server:
  command: node
  args: [dist/entry.js]
assert:
  tool: hudu_test_connection
  args: {}
  expect:
    is_error: true
timeout: 30s
